# Залежності для Lab1 - Базова система керування проектами
# Цей файл містить список Python пакетів, які потрібно встановити

# pytest - фреймворк для тестування Python коду
# Використовується для автоматичного запуску тестів
pytest>=7.0.0

# pytest-cov - плагін для вимірювання покриття коду тестами
# Показує які частини коду протестовані, а які ні
pytest-cov>=4.0.0
